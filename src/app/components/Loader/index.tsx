import React from 'react';
import { LoaderContainer, LoadingText } from './styles';

const Loader = () => (
  <LoaderContainer>
    <LoadingText>
      Caricamento
    </LoadingText>
  </LoaderContainer>
);

export default Loader;
